# W-004 adjudicated Design / UI composition regression v2

Created: 2026-09-02 after v1 execution.

This suite is a regression suite, not fresh `valid_unseen` evidence. It copies
the frozen v1 cases and changes only the required reference set for
`ui-comp-test-bounded-design-with-system`. Independent SOL adjudication found
that responsive and state mechanics require `ui-quality` in addition to
Framework and Validation under the written COMPOSED router. v1 incorrectly
forbade that read.

The v1 benchmark, lock, failed row, and original Gold remain unchanged. v2 may
verify behavior after the adjudication but cannot restore unseen status or
replace the independent W-005 holdout.

